[project]
name = "career-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "openai",
    "chromadb",
    "pydantic",
    "python-dotenv",
    "tiktoken",
    "langchain-text-splitters",
    "gradio",
]
